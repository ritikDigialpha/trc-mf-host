import React from "react";

const Footer = ({ body }: any) => {
  return (
    <div style={{ width: "100%", height: "50px", background: "blue" }}>
      Footer by main by ashok
      {body}
    </div>
  );
};

export default Footer;
